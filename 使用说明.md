# 「付加旺 · 个人主页」使用说明

> 这是一份**写给你自己看**的说明书。
> 你不需要懂前端，只要照着下面四条路径改，网站就能一直更新下去。

---

## 0 · 先搞清楚这个文件夹里有什么

| 文件 | 是什么 | 你要不要动它 |
|---|---|---|
| `index.html` | **成品站**。双击就能在浏览器打开，全部内容 + 样式 + 逻辑都在这一个文件里 | ✅ 你要改的就是它 |
| `index.full.html` | 同一个站的「注释加强版」，改法和 `index.html` 完全一样 | ⭕ 想看详细注释时用它 |
| `使用说明.md` | 本文件 | 不用动 |

**重要：整个网站只有 `index.html` 一个文件就能跑。**
把 `index.html` 单独发给别人、拷到 U 盘、传到 GitHub Pages，都能正常打开。

---

## 1 · 怎么打开

直接**双击 `index.html`**，浏览器就会打开。

不需要装任何东西，不需要联网，不需要命令行。
站点默认**完全不请求任何外部资源**（连字体 CDN 都已关闭），断网、离线、拷 U 盘都能用。
想要更好看的英文科技字体（Orbitron 等）需要手动开启，方法见下面「5 · 离线能力与字体」。

---

## 2 · 三条最常走的修改路径（这就是「可继续修改」的保证）

### 🅰️ 路径一：改内容（改名字 / 改城市 / 加项目 / 加技能）

**改 1 处，全站生效。**

1. 用记事本、VS Code 或任意文本编辑器打开 `index.html`
2. 按 `Ctrl + F`，搜索：**`const PROFILE`**
3. 你会看到一大块配置，长这样：

```javascript
const PROFILE = {
  identity: {
    name: '付加旺',                    // 改这里 → 页面上所有出现名字的地方都会变
    city: '江苏 · 镇江',                // 改这里 → 坐标会变
    ...
```

4. 把引号 `' '` **里面**的字改掉，**不要删引号和逗号**
5. `Ctrl + S` 保存 → 回到浏览器按 `F5` 刷新 → 改好了

**几个常见改法：**

| 想做什么 | 怎么改 |
|---|---|
| 改名字 | 改 `identity.name` 一处，页面上所有位置同步更新 |
| 更新页脚时间 | 改 `updatedAt`（格式 `'YYYY-MM'`，如 `'2026-10'`），页脚「最后更新」随之变化 |
| 加一项技能 | 在 `skills` 数组里加一行 `{ group: '编程与工具', name: '技能名', level: 2, note: '说明' }` |
| 加一张证书 | 在 `achievements` 数组里加一项 `{ name: '证书名', type: '资格证', status: '已获得', note: '说明' }` |
| 加一个兴趣 | 在 `interests` 数组里加一行 `{ name: '摄影', icon: '📷', desc: '说明' }` |
| 加一段教育经历 | 在 `education` 数组里再加一个 `{ ... }`（时间倒序，最新的放最前面） |
| 加一个项目 | 在 `projects` 数组里照抄一份 `{ ... }` 再改内容 |
| 上传头像照片 | 把 `identity.avatar` 从 `''` 改成图片路径，如 `'avatar.jpg'`（图片和 `index.html` 放同一个文件夹），页面会自动把文字头像换成照片 |
| 填上邮箱 | 把 `contact.email` 从 `'${待填写}'` 改成真实邮箱，页脚会自动出现邮箱入口（不填则自动隐藏，绝不会显示"待填写"三个字） |

**⚠️ 三条内容红线（这是本项目的底线，别突破）：**

1. **没有的不要写。** 目前只有「阿里巴巴 AI 初级训练师」一项证书，没有其他奖项。
2. **英语四级要写「已报考，尚未出分」**，还不能写成「已通过」——等出分后再改。
3. **不要写「精通 / 顶尖 / 最强 / 远超同龄人」这类词**。换成有数字的说法，例如
   「使用 React 与 TypeScript 完成了一个完整上线的 Web 应用」，比「精通 React」可信得多。

**关于项目详略档位**：项目配置里有 `detailLevel`，可填三种值：

| 值 | 效果 |
|---|---|
| `'brief'` | 只显示一句话简介，最简洁 |
| `'standard'` | 默认档：显示规模数字 + 技术栈 + 要点 |
| `'deep'` | 在 `standard` 基础上，额外提供「展开详情」按钮，点开看算法与工程细节表格 |

现在是 `'deep'`。想简化，把它改成 `'standard'` 即可。

---

### 🅱️ 路径二：换配色（改强调色 / 加第五套主题）

**换某个主题的强调色 → 只改 1 行。**

1. `Ctrl + F` 搜索：**`[data-theme=`**（找到 CSS 里那四段主题块）
2. 找到你想改的主题，例如赛博霓虹：

```css
[data-theme="cyber-neon"] {
  --bg:        #07030d;
  --accent:    #ff2d95;    ← 改这一行 = 换这套主题的主色
  --accent-2:  #00f0ff;
  ...
```

3. 把 `--accent` 的值换成你喜欢的十六进制颜色（如 `#22d3ee`），保存刷新即可。
   **全站的按钮、发光、标题强调色会一起变。**

**加第五套主题（只需两步，不用碰任何渲染代码）：**

第 1 步：`Ctrl + F` 搜索 `const THEMES`，在里面加一行：

```javascript
const THEMES = {
  'deep-space': { label: '深空冷峻',   desc: '冷峻、克制、信任感', swatch: '#4da3ff' },
  'cyber-neon': { label: '赛博霓虹',   desc: '高能、夜之城',       swatch: '#ff2d95' },
  'geek-code':  { label: '极客代码流', desc: '终端、极简',         swatch: '#33ff66' },
  'hud-panel':  { label: 'HUD 仪表',   desc: '军工、精密',         swatch: '#ffb000' },

  // ↓ 新加的第五套（注意：key 用小写字母和连字符，不要有空格）
  'ocean-blue': { label: '深海蓝',     desc: '安静、深邃',         swatch: '#2ec5ff' }
};
```

第 2 步：`Ctrl + F` 搜索 `[data-theme="hud-panel"]`，在它后面**复制粘贴**一段并改名：

```css
/* ── E · 深海蓝（我新加的） ── */
[data-theme="ocean-blue"] {
  --bg:        #040d14;
  --bg-soft:   #071826;
  --surface:   #0a2133;
  --surface-2: #0e2b41;
  --border:    #15415f;
  --text:      #dcf3ff;
  --text-dim:  #7fa9c2;
  --accent:    #2ec5ff;
  --accent-2:  #8ce8ff;
  --glow:      rgba(46,197,255,.45);
  --grid:      rgba(46,197,255,.06);
  --radius:    12px;
}
```

保存刷新 —— **右上角按钮里会自动多出「深海蓝」这一项**，因为它是由 `THEMES` 自动生成的，你不需要改 HTML。

**想改默认主题**：搜索 `DEFAULT_THEME`，把 `'deep-space'` 换成你想默认显示的那个 key。

---

### 🅲 路径三：隐藏 / 调整板块顺序（改结构）

**板块的「顺序」和「显隐」集中在 `SECTION_CONFIG` 里**（这是「结构层」，与内容、配色分开）。

1. `Ctrl + F` 搜索：**`const SECTION_CONFIG`**
2. 你会看到六个板块各占一行，长这样：

```javascript
const SECTION_CONFIG = [
  { key: 'profile',      title: '个人档案', hidden: false, slots: [ ... ] },
  { key: 'education',    title: '教育背景', hidden: false, slots: [ ... ] },
  ...
];
```

| 想做什么 | 怎么改 |
|---|---|
| **隐藏**一个板块 | 把该行的 `hidden: false` 改成 `hidden: true`（顶部导航会自动少一项） |
| **恢复**一个板块 | 把 `hidden: true` 改回 `false` |
| **调整顺序** | 直接上下调换整行的先后位置（板块与导航一起变） |
| **改板块标题** | 改该行的 `title: '...'`（导航文字同步变化） |

> ⚠️ 只改 `title` / `hidden` / 行的先后顺序即可，**不要动 `key` 和 `slots`**——`key` 是板块的锚点，改了会导致导航跳转失效。
> ⚠️ 六个板块的**默认顺序**是定好的（个人档案 → 教育背景 → 技能矩阵 → 项目作品 → 荣誉资质 → 兴趣偏好），没事别乱调。

---

### 🅳 路径四：部署上线（放到 GitHub Pages，拿到可分享链接）

你的 GitHub 用户名是 `fjw66666666`，可以直接沿用现有的部署习惯。
**目标**：得到一个形如 `https://fjw66666666.github.io/resume/` 的公开链接，能直接发微信。

#### 第 1 步 · 新建仓库并上传

1. 打开 https://github.com/new
2. 仓库名建议填 **`resume`**（或任意你喜欢的名字）
3. 可见性选 **Public**（Pages 免费版要求公开仓库）
4. 点 `Create repository`
5. 进入新仓库，点 `Add file` → `Upload files`
6. 把 `index.html` **拖进去**（注意：只传 `index.html` 这一个文件就够）
7. 点下方 `Commit changes`

#### 第 2 步 · 开启 Pages

1. 仓库页面 → 上方 `Settings`
2. 左侧栏找到 `Pages`
3. `Source` 选 **`Deploy from a branch`**
4. `Branch` 选 **`main`**，目录选 **`/ (root)`**
5. 点 `Save`
6. 等 **1–2 分钟**（第一次会慢一点），刷新这个页面，顶部会出现你的网址

#### 第 3 步 · 拿到链接并分享

链接格式固定为：

```
https://<你的用户名>.github.io/<仓库名>/
```

例如仓库名叫 `resume`，链接就是：

```
https://fjw66666666.github.io/resume/
```

**把这个链接发给任何人，手机、电脑都能打开。**

> **顺手做一件小事（可选）**：拿到链接后，用 `Ctrl + F` 搜索 `og:url`，把它后面引号里的
> `${部署后的链接，留占位}` 换成你的真实链接（例如 `https://fjw66666666.github.io/resume/`）。
> 这是给微信 / QQ 抓取分享卡片用的标签，填上后分享效果更完整。**不填也不影响页面正常打开。**

#### 📱 微信打开与分享的注意点

| 情况 | 说明与对策 |
|---|---|
| 微信里直接打开 `github.io` 链接 | ✅ 通常可直接打开，站点已做微信内置浏览器兼容 |
| 分享到聊天/群时提示「非官方网页」或安全校验 | 这是微信对第三方域名的常规提示，**点「继续访问」即可**；接收者若打不开，让对方**在手机浏览器（Safari / Chrome）里打开**同一链接 |
| 想彻底避免微信提示 | 需绑定**自有域名**（如从腾讯云/阿里云买一个域名，在仓库 `Settings → Pages → Custom domain` 填入并配置 DNS）——这一步是可选进阶操作 |
| 页面在微信里显示不全 / 底部被裁 | 已做 `100dvh` 适配，正常情况下不会；如仍出现，让对方**下拉刷新**一次 |

#### 🔄 以后怎么更新内容

改完本地的 `index.html` → 回到 GitHub 仓库 → `Add file` → `Upload files` → 重新上传覆盖 → `Commit changes`。
等 1 分钟，链接内容就更新了（**链接本身不变**，所以已经发出去的链接不用重发）。

---

## 3 · 手机端 / 微信兼容说明

本站已针对手机与微信内置浏览器做过适配，具体如下：

| 项目 | 做了什么 |
|---|---|
| **布局** | 320 / 375 / 390 / 414 / 768 / 1440 / 1920 px 七档宽度均已适配，**任何宽度都不会出现横向滚动** |
| **毛玻璃降级** | 部分微信 Android 内核不支持 `backdrop-filter`（毛玻璃）。已加 `@supports` 降级：不支持时改用不透明背景，**只是少了模糊效果，内容依然清晰可读** |
| **中文字体** | 字体栈已显式包含 `PingFang SC` / `Hiragino Sans GB` / `Microsoft YaHei`，微信里中文不会变成难看的默认字体 |
| **滚动抖动** | 顶部悬浮栏加了 `translateZ(0)` 合成层提升，避免微信 X5 内核滚动时抖动 |
| **全屏高度** | 使用 `100vh` + `100dvh` 双写，避免 iOS / 微信地址栏把底部裁掉 |
| **点击区域** | 主题切换按钮保证 **≥ 44×44px**（Apple 人机界面指南标准），拇指易点 |
| **小字号保护** | 部分微信内核会强行放大 <12px 的文字，已把关键小字统一抬到 12px 下限 |
| **移动端交互** | 触屏设备上取消了会"粘住"的 hover 高亮效果 |

> ⚠️ **诚实说明**：以上兼容措施基于标准 Web 规范与微信 X5 已知问题编写，**未在真机上逐一验证所有微信版本**。若你在某台设备上发现异常，把机型 + 微信版本告诉工程师，可针对性修复。


## 4 · 关于那把「悬浮的主题切换按钮」

- **位置**：右上角，**滚动页面也不会消失**。
- **用法**：点一下展开四套主题，点任意一项立即换肤；当前主题会打勾。
- **记忆**：选择会保存在浏览器里，**刷新、关掉重开都还记得**。
- **手机端**：按钮自动收窄成只剩四个色点，点一下照样能展开。
- **无 JS 时**：默认主题（深空冷峻）依然正常显示，页面内容完整。
- **键盘操作**：`Tab` 可以聚焦到按钮，`Enter` 展开，`Esc` 收起。

---

## 5 · 离线能力与字体

- **默认状态已是「极致离线」**：字体 CDN 的 3 行 `<link>` 已被注释关闭，
  站点**不请求任何外部网络**，双击即用；断网、离线、拷 U 盘都不受影响。
- **如何开启科技字体（Orbitron / Inter / JetBrains Mono）**：
  打开 `index.html`，`Ctrl + F` 搜索 `fonts.googleapis.com`，找到那段注释块，
  按注释里的说明**删除注释标记、只保留 3 行 `<link>`**，保存即可。
  字体栈已把这三个字体排在首位，加载成功自动生效，**其余代码一行都不用改**。
- **字体只是「锦上添花」**：不开启时浏览器自动使用系统字体（字体栈已含
  `PingFang SC` / `Microsoft YaHei` 等中文兜底），**功能与布局完全不受影响**；
  即使开启后 CDN 加载失败，也会自动回落系统字体，站点照常可用。

---

## 6 · 这份站点的「可维护性承诺」（你可以自己验证）

| 场景 | 承诺 | 你怎么验 |
|---|---|---|
| 改名字 | 改 `identity.name` 1 处（另同步 `<head>` 的 `og:title`） | 全局搜索「付加旺」：配置区 1 处 + `og:title` 1 处 |
| 加一个项目 | 只在 `projects` 加 1 项，不改 CSS | 加完刷新，卡片自动出现 |
| 加一个技能 | 只在 `skills` 加 1 项 | 同上 |
| 加一个兴趣 | 只在 `interests` 加 1 项 | 卡片自动出现 |
| 加一张证书 | 只在 `achievements` 加 1 项 | 卡片数量自动增加 |
| 换主题主色 | 只改 1 个 CSS 变量 | 全站强调色同步变化 |
| 加第五套主题 | 加 1 个 `THEMES` 条目 + 1 段 CSS | 按钮自动多一项，切换生效 |
| 改默认主题 | 只改 `DEFAULT_THEME` 1 处 | 刷新后默认主题变化 |
| 更新页脚时间 | 只改 `updatedAt` 1 处（`'YYYY-MM'`） | 页脚「最后更新」同步变化 |
| 隐藏 / 调序板块 | 改 `SECTION_CONFIG` 里的 `hidden` 或行顺序 | 板块与导航一起变化，无报错 |
| 可离线打开 | 双击 HTML 即可完整呈现 | 断网测试一遍 |
| 手机可用 | 320 / 375 / 390 / 414 / 768 / 1440 / 1920 px 七档均无横向滚动 | 开发者工具逐档切宽度 |
| 微信可打开 | 微信内置浏览器可正常浏览 | 把链接发到微信，点开测试 |
| 无控制台报错 | Console 里没有红色 error | F12 打开 Console 看 |

---

## 7 · 遇到问题怎么办

| 现象 | 原因 | 解决 |
|---|---|---|
| 页面一片空白 | 配置区某处引号/逗号被删了 | 按 `F12` 看 Console 的报错行号，对照配置区检查；或还原备份重新改一遍 |
| 改了没变化 | 忘了保存，或浏览器缓存 | `Ctrl + S` 保存后按 `Ctrl + F5` 强制刷新 |
| 主题切换点了没反应 | CSS 里的 `[data-theme="xxx"]` 与 `THEMES` 里的 key 拼写不一致 | 两边逐字比对（都要小写、用连字符） |
| 某些字看不见 | 某个颜色变量值填错了 | 检查对应主题块里有没有漏掉分号 |

**改之前先备份一份 `index.html`**（复制成 `index.bak.html`），这是最省事的保险。

---

## 8 · 一个诚实的提醒

本站默认使用系统字体，不依赖任何外部资源；即使你手动开启了字体 CDN，加载失败时也会自动降级回系统字体，站点照常可用——这正是「零依赖 + 强制降级」设计的意义：**五年后打开，它还是这个样子。**
